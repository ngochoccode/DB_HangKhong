# DB_HangKhong
// Cấu hình tên, mail
git config --global user.name "name" 
git config --global user.email "mail"
// Clone repo về máy
git clone URL
// Tạo branch mới và switch vào branch này
git branch "name"
Git checkout "name"
// Add branch mới và commit lên branch
git add .
git commit -m "Tạo branch tên branch"
git push --set-upstream origin ngoc

