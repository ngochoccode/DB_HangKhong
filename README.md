# DB_HangKhong
git config --global user.name "name" 
git config --global user.email "mail"
Git clone URL
Git branch "name"
Git checkout "name"
Git add .
Git commit -m "Tạo branch tên branch"
Git push
